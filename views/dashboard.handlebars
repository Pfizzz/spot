<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <h1>Welcome!</h1>
    <form action="/user" method="POST"
<div class="container orange">
    <p class="white-text">Please change your username or password below, or you may return to the homepage.</p>
 <form action="/user" method="POST"
  <div class="form-group">
    <label for="username" class="white-text"> Change username:</label>
    <input type="text" value="{{username}}" class="form-control" name="password" id="username-update" />
  </div>
  <div class="form-group">
    <label for="password" class="white-text">Change password:</label>
    <input type="password" value="{{password}}"class="form-control" name="password" id="password-update" />
  </div>
  <div>
    <button class="btn waves-effect waves-light pulse purple accent-4 white-text" type="submit">Update</button>
  </div>
  <br/>
   <div>
 <p><a href="profile/{{username}}" target=" _blank"></a>Veiw your profile</p>
  </div>
</form>
</body>
</html>
   